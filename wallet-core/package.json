{
  "name": "@nomad/wallet-core",
  "version": "0.1.0",
  "description": "Wallet core primitives for Nomad (seed, derivation, encryption)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -w -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT",
  "dependencies": {
    "ethers": "^6.0.0",
    "@scure/bip39": "^1.0.0",
    "bitcoinjs-lib": "^6.1.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
